import { CardType } from "../../../common";
import { IPost } from "../dtos";

export interface IUserCardConfig extends IPost {
  // type?: CardType;
  // username: string;
  // avatar: string;
  // timeAgo: string;
  // supportRequested: boolean;
  // grateful: boolean;
  // isFollowed: boolean;
  // favouriteCount: number;
  // commentCount: number;
  // addCount: number;
  // isCommented: boolean;
  // hasBorder?: boolean;
  // isEnableCommented?: boolean;
  // postContent: string;
}
